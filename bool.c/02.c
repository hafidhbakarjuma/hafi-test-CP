#include <stdio.h>
#include <stdbool.h>

int main ()
{
// We can use bool like variable
// Also you need to mention <stdbool.h> in order to use bool
bool a = 0;
bool b = 1;
    if(a > b) printf("true ");
    else printf("false ");


    return 0;
}